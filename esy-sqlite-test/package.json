{
  "esy": {
    "build": [
      ["make", "TARGET_DIR=#{self.target_dir}"]
    ]
  },
  "scripts": {
    "test": "make test TARGET_DIR=#{self.target_dir}"
  },
  "dependencies": {
    "esy-sqlite": "hyperag/esy-sqlite:package.json#b353f2b"
  }
}
